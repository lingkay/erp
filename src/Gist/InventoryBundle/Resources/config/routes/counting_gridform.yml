
gist_inv_counting_gridform_index:
    pattern: /list
    methods: [GET]
    defaults: { _controller: GistInventoryBundle:CountingForm:index }

gist_inv_counting_gridform_index_submit:
    pattern: /list
    methods: [POST]
    defaults: { _controller: GistInventoryBundle:CountingForm:indexSubmit }

